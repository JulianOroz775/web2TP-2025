<?php
$title = $title ?? 'Lista de Suplementos';
$isLogged = false;

if (isset($this) && property_exists($this, 'user') && !empty($this->user)) {
  $isLogged = true;
} else {
  if (session_status() === PHP_SESSION_NONE) {
    session_start();
  }
  $isLogged = !empty($_SESSION['ID_USER'] ?? null);
}
?>
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous"
  />

  <link rel="stylesheet" href="<?= BASE_URL ?? '' ?>css/style.css" />

  <title><?= htmlspecialchars($title) ?></title>
</head>

<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="nav-link" href="<?= BASE_URL ?>">Marcas</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNav" aria-controls="navbarNav"
                aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="<?= BASE_URL ?>suplementos">Productos</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<?= BASE_URL ?>showLogin">Admin</a>
            </li>

            <?php if ($isLogged): ?>
                <li class="nav-item">
                    <a class="nav-link" href="<?= BASE_URL ?>logout">Salir</a>
                </li>            
            <?php endif; ?>

          </ul>
        </div>
      </div>
    </nav>
  </header>
